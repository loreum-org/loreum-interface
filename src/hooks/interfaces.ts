export interface ERC20 {
    token_address: string,
    symbol: string,
    name: string,
    logo: string,
    thumbnail: number,
    decimals: string,
    balance: string,
    possible_spam: boolean,
    verified_collection: boolean,
}